{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml"
  xmlns:video="http://www.google.com/schemas/sitemap-video/1.1">
  {{- range .Pages }}
  {{- $page := . }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
    {{- if .IsTranslated }}
      {{- range .AllTranslations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.Locale }}" href="{{ .Permalink }}" />
      {{- end }}
    {{- end }}
    {{- with .Params.video }}
    <video:video>
      <video:thumbnail_loc>{{ $page.Params.videoThumbnail | default ($page.Params.image | absURL) }}</video:thumbnail_loc>
      <video:title>{{ $page.Title }}</video:title>
      <video:description>{{ $page.Description | default $page.Title }}</video:description>
      <video:content_loc>{{ . | absURL }}</video:content_loc>
      <video:publication_date>{{ $page.PublishDate.Format "2006-01-02T15:04:05-07:00" }}</video:publication_date>
      <video:family_friendly>no</video:family_friendly>
    </video:video>
    {{- end }}
  </url>
  {{- end }}
</urlset>
